[pytest]
markers =
    integration: marks tests as integration tests (deselect with '-m "not integration"')
    requires_api: Tests that require real API keys (expensive)
    performance: Performance and quality gate tests (excluded by default)
    timeout: Marks tests with a timeout constraint
addopts = -m "not performance" -k "not performance"
